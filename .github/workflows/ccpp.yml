name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: configure
      run: cmake -DCMAKE_CXX_FLAGS="-Werror" hw1/
    - name: make
      run: cd hw1 && make
